# Ignore Outputs contents but keep the folder
/Outputs/*
!/Outputs/.gitkeep

# Ignore root-level config files
# Settings.yml is tracked so WARP0716 dark theme is default
/LastSession.yml

# Ignore Skipped Patches logs
SkippedPatches.log
